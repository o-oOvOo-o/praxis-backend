load("//:defs.bzl", "praxis_rust_crate")

praxis_rust_crate(
    name = "common",
    crate_name = "core_test_support",
    crate_srcs = glob(["*.rs"]),
    lib_data_extra = [
        "//praxis-rs/core:model_availability_nux_fixtures",
    ],
)
